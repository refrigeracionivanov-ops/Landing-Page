/// <reference types="@sanity/astro/module" />

// `App.Locals` (con `locals.runtime.env`) lo declara @astrojs/cloudflare.
// La interfaz global `Env` con los bindings sale de `worker-configuration.d.ts`,
// que se regenera con `npm run tipos` cada vez que cambia wrangler.jsonc.
